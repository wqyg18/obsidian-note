首先确保公式中选择 `latex`

快捷键: 
+ `Ctrl Shift =` => `线性公式`
+ `Ctrl =` => `专业公式`

```latex
{\widetilde{x}}_{hard}=\cases{x\emsp&if\left|x\right|>\epsilon\cr0\emsp&if\left|x\right|\le\epsilon\cr}
```

大括号对应`\cases`, 通过`\cr`换行, 通过`\quad`打出空格