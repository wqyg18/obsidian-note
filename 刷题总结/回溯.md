```python
def backtrack():
    if 到达终点:
        收集答案
        return

    for 选择 in 所有选择:
        if 选择不合法:
            continue

        做选择
        backtrack()
        撤销选择
```
注意矩阵中, 数字存的是int还是str

DFS：一般用递归，也可以用栈
BFS：一般用队列

```python
directions = [(-1, 0), (1, 0), (0, -1), (0, 1)]
```
